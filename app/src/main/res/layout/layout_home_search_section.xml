<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/homePageSearchSection"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">


        <!-- Left Space -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"/>


        <!-- Search Text and Button -->
        <RelativeLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="8">

            <ImageButton
                android:id="@+id/home_search_button"
                style="@style/commonCircleButton"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:src="@drawable/button_image_search"
                app:tint="@color/textOverColor"
                android:tintMode="src_in"
                android:contentDescription="@string/content_desc_search"/>

            <EditText
                android:id="@+id/home_search_text"
                style="@style/commonEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/home_search_button"
                android:layout_centerVertical="true"
                android:textAlignment="center"
                android:hint="@string/label_search"
                tools:ignore="Autofill"/>

        </RelativeLayout>


        <!-- Right Space -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"/>


    </LinearLayout>

</LinearLayout>